{
  "name": "react-drawer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "turbo run dev",
    "dev:package": "turbo run dev --filter react-drawer-component",
    "dev:example": "turbo run dev --filter examples",
    "build": "turbo run build",
    "build:package": "turbo run build --filter react-drawer-component",
    "build:example": "turbo run build --filter example"
  },
  "devDependencies": {
    "turbo": "^1.11.0"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
